# Решение проблемы "Failed to fetch"

## ✅ Что уже исправлено:

1. ✅ Восстановлен `main.ts` с настройками CORS
2. ✅ CORS настроен для портов 3000 и 3001
3. ✅ Улучшена обработка ошибок (автоматический редирект на логин при 401)
4. ✅ Серверы запущены:
   - Backend API: http://localhost:4000/api
   - Frontend: http://localhost:3000

## 🔍 Диагностика проблемы:

### Шаг 1: Проверьте, что вы залогинены

1. Откройте консоль браузера (F12)
2. Перейдите на вкладку "Application" (Chrome) или "Storage" (Firefox)
3. Найдите "Local Storage" → `http://localhost:3000`
4. Проверьте наличие ключей:
   - `token` - должен содержать JWT токен
   - `user` - должен содержать данные пользователя

**Если токена нет:**
- Войдите в систему заново:
  - Логин: `investor1`
  - Пароль: `investor123`

### Шаг 2: Проверьте консоль браузера

Откройте консоль (F12) и проверьте ошибки:
- Если видите `Failed to fetch` - проблема с подключением к API
- Если видите `401 Unauthorized` - токен истек или неверный
- Если видите `CORS error` - проблема с CORS (уже исправлено)

### Шаг 3: Проверьте Network вкладку

1. Откройте DevTools (F12)
2. Перейдите на вкладку "Network"
3. Обновите страницу (F5)
4. Найдите запрос к `/api/portfolio/properties`
5. Проверьте:
   - **Status**: должен быть 200 (OK) или 401 (Unauthorized)
   - **Request Headers**: должен быть заголовок `Authorization: Bearer <token>`
   - **Response**: если 401, токен истек - нужно войти заново

### Шаг 4: Проверьте, что API работает

Выполните в терминале:
```bash
curl -X POST http://localhost:4000/api/auth/login \
  -H "Content-Type: application/json" \
  -d '{"login":"investor1","password":"investor123"}'
```

Должен вернуться JSON с `accessToken` и `user`.

## 🛠️ Решения:

### Решение 1: Войдите заново

1. Откройте http://localhost:3000/login
2. Введите:
   - Логин: `investor1`
   - Пароль: `investor123`
3. Нажмите "Войти"
4. После успешного входа вы будете перенаправлены на `/portfolio`

### Решение 2: Очистите localStorage

Если проблема сохраняется:
1. Откройте консоль браузера (F12)
2. Выполните:
```javascript
localStorage.clear();
location.reload();
```
3. Войдите заново

### Решение 3: Перезапустите серверы

Если проблема не решается:
```bash
# Остановите все процессы
pkill -f "nest start"
pkill -f "next dev"

# Запустите заново
cd apps/api && pnpm run start:dev &
cd apps/dashboard && pnpm dev &
```

## 📝 Проверка работы:

После входа в систему:
1. ✅ Токен должен быть в localStorage
2. ✅ Страница `/portfolio` должна загружаться
3. ✅ Если объектов нет, должно быть сообщение "Объекты не найдены" (это нормально)
4. ✅ Не должно быть ошибок "Failed to fetch" в консоли

## ⚠️ Важно:

- Если объектов нет - это нормально! Создайте первый объект через форму создания.
- Если видите "Объекты не найдены" - это означает, что API работает, но данных нет.
- Ошибка "Failed to fetch" означает проблему с подключением, а не с данными.

## 🆘 Если ничего не помогает:

1. Проверьте логи серверов:
   - API: `/tmp/api.log`
   - Dashboard: `/tmp/dashboard.log`
2. Убедитесь, что порты свободны:
   - 4000 - API
   - 3000 - Frontend
3. Проверьте, что база данных существует и доступна

