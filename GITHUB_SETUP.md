# Инструкция по загрузке проекта на GitHub

## ✅ Что уже сделано:
- ✅ Git репозиторий инициализирован
- ✅ Все файлы добавлены и закоммичены
- ✅ Ветка переименована в `main`
- ✅ Создан скрипт для автоматической загрузки

## 📝 Следующие шаги:

### Вариант 1: Использовать готовый скрипт (рекомендуется)

1. **Создайте репозиторий на GitHub:**
   - Перейдите на https://github.com/new
   - Название: `sabay-owner-dashboard` (или любое другое)
   - **НЕ** создавайте README, .gitignore или лицензию
   - Нажмите "Create repository"

2. **Скопируйте URL репозитория** (например: `https://github.com/ваш-username/sabay-owner-dashboard.git`)

3. **Запустите скрипт:**
   ```bash
   cd "/Users/v.goncharov/Desktop/Program/Sabay Owner Dashboard"
   ./PUSH_TO_GITHUB.sh https://github.com/ваш-username/sabay-owner-dashboard.git
   ```

### Вариант 2: Выполнить команды вручную

```bash
cd "/Users/v.goncharov/Desktop/Program/Sabay Owner Dashboard"

# Добавить удаленный репозиторий (замените URL на ваш)
git remote add origin https://github.com/ваш-username/sabay-owner-dashboard.git

# Загрузить код
git push -u origin main
```

## 🔐 Настройка Git (если еще не настроено)

```bash
git config --global user.name "Ваше Имя"
git config --global user.email "ваш@email.com"
```

## ⚠️ Если возникнут проблемы:

1. **Ошибка авторизации:**
   - GitHub больше не поддерживает пароли для HTTPS
   - Используйте Personal Access Token (Settings → Developer settings → Personal access tokens)
   - Или настройте SSH ключи

2. **Репо уже существует:**
   - Скрипт автоматически обновит URL удаленного репозитория

3. **Нужна помощь:**
   - Проверьте: `git remote -v` - должен показать ваш репозиторий
   - Проверьте: `git status` - должен быть "nothing to commit"

