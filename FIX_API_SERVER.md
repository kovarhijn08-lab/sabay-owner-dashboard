# Инструкция по восстановлению API сервера

## Проблема
API сервер не запускается из-за ошибки подключения к базе данных:
```
Error: Could not locate the bindings file
```

## Причина
Проблема с нативными биндингами `better-sqlite3` в pnpm workspace.

## Решение

### Вариант 1: Пересборка better-sqlite3 (рекомендуется)

```bash
# 1. Перейдите в директорию API
cd '/Users/v.goncharov/Desktop/Program/Sabay Owner Dashboard/apps/api'

# 2. Остановите все процессы (если запущены)
pkill -f 'nest.*start'

# 3. Пересоберите better-sqlite3
pnpm rebuild better-sqlite3

# 4. Запустите сервер
pnpm start:dev

# 5. Дождитесь сообщения:
#    "🚀 API is running on http://localhost:4000/api"
```

### Вариант 2: Полная переустановка зависимостей

```bash
# 1. Перейдите в директорию API
cd '/Users/v.goncharov/Desktop/Program/Sabay Owner Dashboard/apps/api'

# 2. Удалите node_modules
rm -rf node_modules

# 3. Переустановите зависимости
pnpm install

# 4. Пересоберите better-sqlite3
pnpm rebuild better-sqlite3

# 5. Запустите сервер
pnpm start:dev
```

### Вариант 3: Использование PostgreSQL (если установлен)

```bash
# 1. Отредактируйте .env файл в apps/api/
DB_TYPE=postgres
DATABASE_URL=postgresql://user:password@localhost:5432/sabay_owner

# 2. Запустите сервер
pnpm start:dev
```

## Проверка

После запуска сервера:

1. Проверьте, что сервер запущен:
   ```bash
   lsof -ti:4000
   ```

2. Проверьте доступность API:
   ```bash
   curl http://localhost:4000/api/health
   ```

3. Обновите страницу в браузере (F5 или Cmd+R)

## База данных

База данных находится в:
- `apps/api/data/database.sqlite` (основная)
- `apps/api/database.sqlite` (резервная)

Данные должны сохраниться после перезапуска сервера.

## Если проблема сохраняется

1. Проверьте версию Node.js:
   ```bash
   node --version
   ```
   Рекомендуется Node.js 18+ или 20+

2. Проверьте версию pnpm:
   ```bash
   pnpm --version
   ```

3. Попробуйте использовать npm вместо pnpm:
   ```bash
   npm install
   npm rebuild better-sqlite3
   npm run start:dev
   ```

