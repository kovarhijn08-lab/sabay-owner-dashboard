# 🔤 Что такое Git алиасы?

## 📖 Простое объяснение

**Алиас** (от английского "alias" - псевдоним) - это **короткое имя для длинной команды**.

Представьте, что вместо того, чтобы каждый раз писать длинную команду, вы можете использовать короткое и удобное имя.

## 🎯 Примеры из реальной жизни

### Без алиасов:
```bash
git status                    # Проверить статус
git checkout -b new-feature   # Создать новую ветку
git add -A && git commit -m "message" && git push  # Сохранить изменения
```

### С алиасами:
```bash
git st                        # То же, что git status
git new new-feature           # То же, что git checkout -b new-feature
git save "message"            # То же, что git add -A && git commit -m "message" && git push
```

**Видите разницу?** Короче, быстрее, удобнее! 🚀

## 🔧 Как работают алиасы?

Алиасы - это просто **сокращения**, которые Git запоминает и заменяет на полные команды.

### Пример создания алиаса:
```bash
git config --global alias.st status
```

Это означает:
- **`st`** - короткое имя (алиас)
- **`status`** - полная команда, которую он заменяет

Теперь когда вы пишете `git st`, Git автоматически выполняет `git status`.

## 📚 Типы алиасов

### 1. Простые алиасы (замена одной команды)
```bash
git config --global alias.st status
# git st → git status
```

### 2. Алиасы с параметрами
```bash
git config --global alias.new "checkout -b"
# git new feature → git checkout -b feature
```

### 3. Сложные алиасы (несколько команд)
```bash
git config --global alias.save "!f() { git add -A && git commit -m \"$1\" && git push; }; f"
# git save "message" → выполняет 3 команды подряд
```

## 🎨 Настроенные алиасы в проекте

### Основные команды:
```bash
git st          # git status - проверить статус
git co          # git checkout - переключиться на ветку
git br          # git branch - работа с ветками
git ci          # git commit - создать коммит
```

### Улучшенные команды:
```bash
git lg          # Красивая история коммитов с цветами и графиком
git ll          # История с графиком всех веток
git last        # Показать последний коммит
```

### Работа с ветками:
```bash
git new feature-name    # Создать новую ветку (git checkout -b)
git switch branch-name   # Переключиться на ветку
git branches            # Показать все ветки
```

### Сохранение:
```bash
git save "описание"     # Добавить все, закоммитить и загрузить
git save-all            # Автоматическое сохранение с датой/временем
```

### Очистка:
```bash
git cleanup             # Удалить слитые ветки
git prune-branches      # Удалить удаленные ветки
```

### Статистика:
```bash
git stats               # Статистика изменений
git contributors        # Список контрибьюторов
```

## 💡 Зачем нужны алиасы?

### ✅ Преимущества:

1. **Экономия времени** ⏱️
   - Меньше печатать
   - Быстрее работать

2. **Удобство** 😊
   - Легче запомнить короткие команды
   - Меньше ошибок при вводе

3. **Стандартизация** 📋
   - Одинаковые команды для всей команды
   - Меньше путаницы

4. **Автоматизация** 🤖
   - Можно объединить несколько команд в одну
   - Сложные операции становятся простыми

## 🔍 Как посмотреть все алиасы?

```bash
# Посмотреть все алиасы
git config --global --list | grep alias

# Или
git config --global --get-regexp alias
```

## 🛠️ Как создать свой алиас?

### Простой алиас:
```bash
git config --global alias.имя "команда"
```

**Пример:**
```bash
git config --global alias.unstage "reset HEAD --"
# Теперь: git unstage file.txt
```

### Алиас с параметрами:
```bash
git config --global alias.имя "команда с $1"
```

**Пример:**
```bash
git config --global alias.new "checkout -b"
# Теперь: git new feature → git checkout -b feature
```

### Сложный алиас (функция):
```bash
git config --global alias.имя "!f() { команды; }; f"
```

**Пример:**
```bash
git config --global alias.save "!f() { git add -A && git commit -m \"$1\" && git push; }; f"
# Теперь: git save "message"
```

## 🗑️ Как удалить алиас?

```bash
git config --global --unset alias.имя
```

**Пример:**
```bash
git config --global --unset alias.st
```

## 📝 Примеры использования

### Ежедневная работа:
```bash
# Проверить статус
git st

# Создать новую ветку для функции
git new feature/carousel

# Внести изменения...

# Сохранить все изменения
git save "feat: добавлена карусель"

# Посмотреть красивую историю
git lg
```

### Сравнение:

**Без алиасов:**
```bash
git status
git checkout -b feature/carousel
git add -A
git commit -m "feat: добавлена карусель"
git push
git log --oneline --graph --all
```

**С алиасами:**
```bash
git st
git new feature/carousel
git save "feat: добавлена карусель"
git lg
```

**Экономия:** 5 команд → 4 команды, и они короче! 🎉

## 🎓 Резюме

**Алиасы** - это:
- 📝 Короткие имена для длинных команд
- ⚡ Экономия времени и усилий
- 🎯 Удобство в ежедневной работе
- 🤖 Возможность автоматизации

**Проще говоря:** вместо `git status` можно писать `git st`, и это работает точно так же!

---

**Все алиасы уже настроены в вашем проекте!** Просто используйте их. ✨

