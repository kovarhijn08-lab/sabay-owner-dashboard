# 🔧 Решение проблемы аутентификации PostgreSQL

## Проблема
Ошибка: `password authentication failed for user "postgres"`

## Возможные решения

### Решение 1: Проверьте правильность пароля

Пароль может быть чувствителен к регистру или содержать скрытые символы. Попробуйте:
1. Скопировать пароль из pgAdmin 4 (если там сохранён)
2. Ввести пароль вручную, проверяя каждый символ

### Решение 2: Использовать другого пользователя

Возможно, нужно использовать ваше системное имя пользователя:

```bash
/Library/PostgreSQL/18/bin/psql -h localhost -U v.goncharov
```

### Решение 3: Временно изменить метод аутентификации (только для разработки!)

**⚠️ ВНИМАНИЕ: Это небезопасно для production!**

1. Найдите файл конфигурации:
   ```bash
   sudo nano /Library/PostgreSQL/18/data/pg_hba.conf
   ```

2. Найдите строки с `host` и `localhost`:
   ```
   host    all             all             127.0.0.1/32            scram-sha-256
   host    all             all             ::1/128                 scram-sha-256
   ```

3. Временно замените на `trust` (только для localhost!):
   ```
   host    all             all             127.0.0.1/32            trust
   host    all             all             ::1/128                 trust
   ```

4. Перезапустите PostgreSQL:
   ```bash
   sudo launchctl unload /Library/LaunchDaemons/com.edb.launchd.postgresql-18.plist
   sudo launchctl load /Library/LaunchDaemons/com.edb.launchd.postgresql-18.plist
   ```

5. Теперь подключитесь без пароля:
   ```bash
   /Library/PostgreSQL/18/bin/psql -h localhost -U postgres
   ```

6. Установите пароль:
   ```sql
   ALTER USER postgres PASSWORD 'a7NGEr2k!';
   \q
   ```

7. **Верните настройки pg_hba.conf обратно на scram-sha-256!**

### Решение 4: Использовать pgAdmin для проверки

1. Откройте pgAdmin 4
2. Посмотрите сохранённые серверы
3. Проверьте, какой пользователь и пароль там используются
4. Попробуйте подключиться через pgAdmin

### Решение 5: Сбросить пароль через pgAdmin

1. Откройте pgAdmin 4
2. Подключитесь к серверу (если есть сохранённое подключение)
3. Правый клик на пользователе `postgres` → Properties → Definition
4. Установите новый пароль: `a7NGEr2k!`
5. Сохраните

## После успешного подключения

1. Создайте базу данных:
   ```sql
   CREATE DATABASE sabay_owner_db;
   \q
   ```

2. Обновите `.env.local`:
   ```env
   DATABASE_URL=postgresql://postgres:a7NGEr2k%21@localhost:5432/sabay_owner_db
   ```
   (Обратите внимание: `!` экранируется как `%21`)

3. Инициализируйте данные:
   ```bash
   cd apps/api
   pnpm init-db
   ```

## Рекомендация

**Лучший вариант:** Используйте pgAdmin 4 для проверки и установки пароля - это самый безопасный способ.
