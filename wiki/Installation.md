# 📦 Установка и настройка

## Требования

### Системные требования
- **Node.js**: версия 18.0 или выше
- **pnpm**: версия 8.0 или выше (рекомендуется) или npm
- **PostgreSQL**: версия 14+ (для продакшена)
- **SQLite**: встроен в Node.js (для разработки)

### Проверка версий

```bash
node --version  # Должно быть >= 18.0.0
pnpm --version  # Должно быть >= 8.0.0
```

## Установка

### 1. Клонирование репозитория

```bash
git clone https://github.com/kovarhijn08-lab/sabay-owner-dashboard.git
cd sabay-owner-dashboard
```

### 2. Установка зависимостей

```bash
# Используя pnpm (рекомендуется)
pnpm install

# Или используя npm
npm install
```

### 3. Настройка переменных окружения

```bash
# Скопировать пример файла окружения
cp apps/api/env.example apps/api/.env

# Отредактировать .env файл
nano apps/api/.env  # или используйте любой редактор
```

### 4. Настройка базы данных

#### Для разработки (SQLite)
SQLite используется по умолчанию, дополнительная настройка не требуется.

#### Для продакшена (PostgreSQL)
1. Установите PostgreSQL
2. Создайте базу данных:
```sql
CREATE DATABASE sabay_owner_db;
```
3. Настройте переменные в `.env`:
```env
DB_TYPE=postgres
DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=your_username
DB_PASSWORD=your_password
DB_DATABASE=sabay_owner_db
```

### 5. Инициализация базы данных

```bash
cd apps/api
pnpm run setup:db
```

### 6. Запуск проекта

```bash
# Из корневой директории
pnpm dev

# Или запустить отдельно
cd apps/dashboard && pnpm dev  # Frontend на http://localhost:3001
cd apps/api && pnpm start:dev  # Backend на http://localhost:3000
```

## Проверка установки

1. Откройте браузер: http://localhost:3001
2. Должна открыться страница входа
3. Проверьте API: http://localhost:3000/api/health

## Следующие шаги

- [Быстрый старт](./Quick-Start)
- [Настройка базы данных](./Database-Setup)
- [Конфигурация](./Configuration)

