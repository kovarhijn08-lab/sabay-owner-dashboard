# 🗄️ Настройка базы данных

## Выбор базы данных

Проект поддерживает два варианта:
- **SQLite** - для разработки (по умолчанию)
- **PostgreSQL** - для продакшена

## SQLite (Разработка)

### Преимущества
- Не требует установки
- Быстрая настройка
- Идеально для разработки

### Настройка
Никакой дополнительной настройки не требуется. SQLite используется по умолчанию.

База данных создается автоматически в:
- `apps/api/database.sqlite`
- `apps/api/data/database.sqlite`

## PostgreSQL (Продакшен)

### Установка PostgreSQL

#### macOS
```bash
brew install postgresql@14
brew services start postgresql@14
```

#### Linux (Ubuntu/Debian)
```bash
sudo apt update
sudo apt install postgresql postgresql-contrib
sudo systemctl start postgresql
```

#### Windows
Скачайте установщик с [официального сайта](https://www.postgresql.org/download/windows/)

### Создание базы данных

```bash
# Войти в PostgreSQL
psql -U postgres

# Создать базу данных
CREATE DATABASE sabay_owner_db;

# Создать пользователя (опционально)
CREATE USER sabay_user WITH PASSWORD 'your_password';
GRANT ALL PRIVILEGES ON DATABASE sabay_owner_db TO sabay_user;

# Выйти
\q
```

### Настройка переменных окружения

Отредактируйте `apps/api/.env`:

```env
DB_TYPE=postgres
DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=sabay_user
DB_PASSWORD=your_password
DB_DATABASE=sabay_owner_db
```

### Инициализация базы данных

```bash
cd apps/api
pnpm run setup:db
```

## Миграции

### Создание таблиц

Таблицы создаются автоматически при первом запуске или через:

```bash
cd apps/api
pnpm run create:tables
```

### Добавление тестовых данных

```bash
pnpm run add:test-data
```

## Резервное копирование

### SQLite
```bash
# Создать копию
cp apps/api/database.sqlite apps/api/database.sqlite.backup

# Восстановить
cp apps/api/database.sqlite.backup apps/api/database.sqlite
```

### PostgreSQL
```bash
# Создать дамп
pg_dump -U sabay_user sabay_owner_db > backup.sql

# Восстановить
psql -U sabay_user sabay_owner_db < backup.sql
```

## Устранение проблем

### Ошибка подключения к PostgreSQL
1. Проверьте, что PostgreSQL запущен:
```bash
# macOS
brew services list

# Linux
sudo systemctl status postgresql
```

2. Проверьте настройки в `.env`
3. Проверьте права пользователя

### База данных не создается
1. Убедитесь, что у пользователя есть права на создание БД
2. Проверьте логи в консоли
3. Попробуйте создать БД вручную

### Миграции не применяются
1. Проверьте подключение к БД
2. Убедитесь, что таблицы не существуют
3. Проверьте логи ошибок

## Полезные команды

```bash
# Инициализация БД
pnpm run setup:db

# Создание таблиц
pnpm run create:tables

# Добавление тестовых данных
pnpm run add:test-data

# Создание пользователей
pnpm run create:admin
pnpm run create:owner
```

## Следующие шаги

- [Конфигурация](./Configuration)
- [Развертывание](./Deployment)
- [Troubleshooting](./Troubleshooting)

