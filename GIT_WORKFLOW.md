# 🔄 Рабочий процесс с Git - Синхронизация изменений

## ✅ Текущий статус:
- ✅ Репозиторий подключен: `https://github.com/kovarhijn08-lab/sabay-owner-dashboard.git`
- ✅ Ветка: `main`
- ✅ Проект загружен на GitHub

## 📝 Как сохранять изменения на GitHub:

### Вариант 1: Быстрая команда (рекомендуется)

После внесения изменений в проект выполните:

```bash
cd "/Users/v.goncharov/Desktop/Program/Sabay Owner Dashboard"
git add -A
git commit -m "Описание ваших изменений"
git push
```

### Вариант 2: Пошагово

1. **Проверить статус изменений:**
   ```bash
   git status
   ```

2. **Добавить все изменения:**
   ```bash
   git add -A
   ```
   Или добавить конкретные файлы:
   ```bash
   git add apps/dashboard/src/app/portfolio/page.tsx
   ```

3. **Создать коммит с описанием:**
   ```bash
   git commit -m "Описание того, что было изменено"
   ```
   Примеры хороших сообщений:
   - `"Fix: исправлена ошибка в карусели"`
   - `"Feature: добавлена новая страница портфеля"`
   - `"Update: обновлены стили для мобильной версии"`

4. **Загрузить на GitHub:**
   ```bash
   git push
   ```

## 🔍 Полезные команды:

### Проверить статус:
```bash
git status
```

### Посмотреть историю коммитов:
```bash
git log --oneline -10
```

### Посмотреть изменения в файлах:
```bash
git diff
```

### Отменить изменения (если что-то пошло не так):
```bash
# Отменить изменения в конкретном файле
git restore имя_файла

# Отменить все незакоммиченные изменения
git restore .
```

## ⚠️ Важно:

1. **База данных (database.sqlite)** - автоматически игнорируется через .gitignore
2. **node_modules/** - не коммитятся (игнорируются)
3. **Секретные данные** - никогда не коммитьте файлы с паролями или токенами

## 🚀 Автоматизация (опционально):

### Создать алиас для быстрого сохранения:

```bash
# Добавить в ~/.zshrc или ~/.bashrc
alias gsave='cd "/Users/v.goncharov/Desktop/Program/Sabay Owner Dashboard" && git add -A && git commit -m "Auto-save: $(date +%Y-%m-%d\ %H:%M:%S)" && git push'
```

После этого можно просто выполнять:
```bash
gsave
```

## 📋 Чек-лист перед каждым коммитом:

- [ ] Проверил изменения: `git status`
- [ ] Добавил файлы: `git add -A`
- [ ] Написал понятное описание: `git commit -m "..."`
- [ ] Загрузил на GitHub: `git push`

## 🔗 Ваш репозиторий:
https://github.com/kovarhijn08-lab/sabay-owner-dashboard

