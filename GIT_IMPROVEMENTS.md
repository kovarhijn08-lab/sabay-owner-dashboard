# 🚀 Улучшения работы с Git

## ✅ Что было добавлено:

### 1. 📝 Шаблон коммитов (`.gitmessage`)
- Стандартизированные сообщения коммитов
- Типы коммитов: feat, fix, docs, style, refactor, perf, test, chore
- Автоматически используется при `git commit`

**Использование:**
```bash
git commit  # Откроется редактор с шаблоном
```

### 2. 📋 Шаблоны для Issues
- **Bug Report** (`.github/ISSUE_TEMPLATE/bug_report.md`)
- **Feature Request** (`.github/ISSUE_TEMPLATE/feature_request.md`)

**Использование:**
При создании Issue на GitHub выберите соответствующий шаблон.

### 3. 🔄 Шаблон для Pull Requests
- `.github/pull_request_template.md`
- Стандартизированная форма для PR

**Использование:**
Автоматически заполняется при создании Pull Request.

### 4. ⚙️ Git Attributes (`.gitattributes`)
- Автоматическая обработка файлов
- Правильные окончания строк (LF)
- Определение бинарных файлов
- Настройка для GitHub Linguist

### 5. 🔧 Git Aliases (скрипт `git-aliases.sh`)
Полезные алиасы для быстрой работы:

```bash
# Настроить алиасы
source git-aliases.sh

# Использование:
git st          # git status
git co          # git checkout
git save "msg"  # git add -A && git commit -m "msg" && git push
git lg          # красивая история коммитов
git ll          # история с графиком
git new branch  # создать новую ветку
git sync        # pull + push
```

### 6. 🤖 GitHub Actions CI/CD
- Автоматическая проверка кода (lint)
- Автоматическая сборка проекта
- Запускается при каждом push и PR

## 🎯 Как использовать:

### Настроить Git алиасы:
```bash
cd "/Users/v.goncharov/Desktop/Program/Sabay Owner Dashboard"
source git-aliases.sh
```

### Использовать шаблон коммитов:
```bash
git commit  # Откроется редактор с шаблоном
```

### Создать Issue с шаблоном:
1. Перейдите на GitHub
2. Issues → New Issue
3. Выберите шаблон (Bug Report или Feature Request)

### Создать Pull Request:
1. Создайте ветку и внесите изменения
2. Создайте PR на GitHub
3. Шаблон заполнится автоматически

## 📚 Дополнительные улучшения:

### Pre-commit hooks (опционально)
Можно добавить автоматические проверки перед коммитом:
- Проверка кода (ESLint)
- Форматирование (Prettier)
- Проверка типов (TypeScript)

### Commitizen (опционально)
Интерактивное создание коммитов:
```bash
npm install -g commitizen
```

## 🔗 Полезные ссылки:

- [Conventional Commits](https://www.conventionalcommits.org/)
- [Git Best Practices](https://github.com/git/git/blob/master/Documentation/howto/maintain-git.txt)
- [GitHub Actions](https://docs.github.com/en/actions)

---

**Все улучшения сохранены и готовы к использованию!** ✨

