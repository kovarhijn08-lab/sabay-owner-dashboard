# 📋 Создание таблиц

## Проблема
Таблицы ещё не созданы. Нужно сначала создать их.

## Решение: Запустить API

API автоматически создаст все таблицы при запуске.

### Шаг 1: Запустите API

Я уже запустил API в фоне. Подождите 10-15 секунд, пока он создаст таблицы.

### Шаг 2: Проверьте таблицы

В pgAdmin выполните:
```sql
\dt
```

Или в Query Tool:
```sql
SELECT table_name FROM information_schema.tables 
WHERE table_schema = 'public';
```

### Шаг 3: После создания таблиц

Выполните SQL из файла INIT_DATA.sql снова.

## Альтернатива: Создать таблицы вручную

Если API не создал таблицы, напишите мне, и я создам SQL для создания всех таблиц вручную.
