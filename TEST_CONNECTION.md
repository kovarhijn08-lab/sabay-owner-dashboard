# 🔍 Тестирование подключения к PostgreSQL

## Проблема
Пароль `a7NGEr2k!` не принимается для пользователя `postgres`.

## Проверка

### 1. Проверьте подключение вручную

```bash
/Library/PostgreSQL/18/bin/psql -h localhost -U postgres
```

Введите пароль `a7NGEr2k!` когда попросит.

### 2. Если не работает, проверьте:

**Вариант A: Другой пользователь**
Возможно, нужно использовать ваше системное имя пользователя:
```bash
/Library/PostgreSQL/18/bin/psql -h localhost -U $(whoami)
```

**Вариант B: Другой хост**
Попробуйте подключиться через 127.0.0.1 вместо localhost:
```bash
/Library/PostgreSQL/18/bin/psql -h 127.0.0.1 -U postgres
```

**Вариант C: Через pgAdmin**
1. Откройте pgAdmin 4
2. Посмотрите сохранённые подключения
3. Проверьте, какой пользователь и пароль используются

### 3. После успешного подключения

Если подключились, создайте базу данных:
```sql
CREATE DATABASE sabay_owner_db;
\q
```

Затем обновите `.env.local` с правильными данными.

### 4. Обновите .env.local

После проверки подключения обновите файл `apps/api/.env.local`:

```env
DATABASE_URL=postgresql://правильный_пользователь:правильный_пароль@localhost:5432/sabay_owner_db
```

**Важно:** В URL пароль нужно экранировать:
- `!` → `%21`
- `@` → `%40`
- `#` → `%23`
- и т.д.

### 5. Инициализируйте данные

```bash
cd apps/api
pnpm init-db
```
