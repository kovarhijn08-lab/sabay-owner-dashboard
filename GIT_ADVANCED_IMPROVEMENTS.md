# 🚀 Продвинутые улучшения для Git

## ✅ Что было добавлено:

### 1. 📝 EditorConfig (`.editorconfig`)
**Для чего:** Единообразие кода во всех редакторах

**Что делает:**
- Автоматически настраивает отступы (2 пробела)
- Устанавливает кодировку UTF-8
- Удаляет пробелы в конце строк
- Добавляет пустую строку в конце файла

**Польза:** Код выглядит одинаково у всех разработчиков, независимо от редактора.

### 2. 🪝 Git Hooks (`.husky/`)
**Для чего:** Автоматические проверки перед коммитом

#### Pre-commit hook:
- ✅ Проверяет, что не коммитятся файлы с секретами (.env, .key)
- ✅ Проверяет размер файлов (не более 10MB)
- ✅ Предупреждает об отладочном коде (console.log, debugger)

#### Commit-msg hook:
- ✅ Проверяет, что сообщение коммита не пустое
- ✅ Предупреждает, если первая строка слишком длинная (>72 символа)

**Польза:** Предотвращает ошибки до того, как они попадут в репозиторий.

### 3. 📋 Changelog (`CHANGELOG.md`)
**Для чего:** История изменений проекта

**Формат:** Keep a Changelog + Semantic Versioning

**Польза:** Всегда видно, что изменилось в проекте.

### 4. 🛠️ Git Helpers (`scripts/git-helpers.sh`)
**Для чего:** Полезные функции для работы с Git

**Доступные функции:**
```bash
source scripts/git-helpers.sh

git_new_branch feature/name     # Создать ветку с проверкой
git_delete_branch branch-name    # Безопасно удалить ветку
git_cleanup_merged               # Удалить все слитые ветки
git_file_history file.tsx        # История изменений файла
git_search_commits "query"       # Поиск в истории коммитов
git_create_release 1.0.0         # Создать тег релиза
git_undo_commit                  # Отменить последний коммит
git_stats                        # Статистика репозитория
```

**Польза:** Упрощает сложные операции с Git.

### 5. ⚙️ Локальная конфигурация (`.gitconfig.local`)
**Для чего:** Настройки Git специально для этого проекта

**Что включает:**
- Настройки цветов
- Настройки diff
- Настройки merge
- Автоматическая настройка удаленного репозитория

## 🎯 Как использовать:

### Настроить Git Hooks:
```bash
# Установить Husky (если еще не установлен)
pnpm add -D husky

# Инициализировать hooks
npx husky install

# Сделать hooks исполняемыми
chmod +x .husky/pre-commit
chmod +x .husky/commit-msg
```

### Использовать Git Helpers:
```bash
# Загрузить функции
source scripts/git-helpers.sh

# Использовать
git_new_branch feature/carousel
git_stats
```

### Применить локальную конфигурацию:
```bash
git config --local include.path ../.gitconfig.local
```

## 📚 Дополнительные улучшения (опционально):

### 1. Commitizen
Интерактивное создание коммитов:
```bash
pnpm add -D commitizen cz-conventional-changelog
echo '{ "path": "cz-conventional-changelog" }' > .czrc
```

Использование:
```bash
git cz  # Вместо git commit
```

### 2. Semantic Release
Автоматическое создание релизов:
```bash
pnpm add -D semantic-release
```

### 3. Git Flow
Структурированный workflow:
```bash
# Установить git-flow
brew install git-flow-avh

# Инициализировать
git flow init
```

### 4. Git LFS
Для больших файлов:
```bash
git lfs install
git lfs track "*.psd"
git lfs track "*.zip"
```

### 5. Prettier + ESLint
Автоматическое форматирование:
```bash
pnpm add -D prettier eslint
```

## 🔍 Проверка работы:

### Проверить EditorConfig:
Откройте файл в любом редакторе - он должен автоматически применить настройки.

### Проверить Hooks:
```bash
# Попробуйте закоммитить файл с секретом
echo "SECRET_KEY=123" > .env.test
git add .env.test
git commit -m "test"  # Должна быть ошибка
```

### Проверить Helpers:
```bash
source scripts/git-helpers.sh
git_stats  # Должна показать статистику
```

## 💡 Рекомендации:

1. **Всегда используйте hooks** - они предотвращают ошибки
2. **Обновляйте Changelog** - при каждом значимом изменении
3. **Используйте helpers** - они упрощают работу
4. **Следуйте EditorConfig** - для единообразия кода

## 🔗 Полезные ссылки:

- [EditorConfig](https://editorconfig.org/)
- [Git Hooks](https://git-scm.com/book/ru/v2/Настройка-Git-Хуки-в-Git)
- [Conventional Commits](https://www.conventionalcommits.org/)
- [Semantic Versioning](https://semver.org/lang/ru/)

---

**Все улучшения готовы к использованию!** ✨

