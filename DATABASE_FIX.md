# Исправление проблемы с загрузкой проектов из базы

## 🔍 Проблема

Проекты из базы данных не подтягивались в каталог - API возвращал пустой массив, хотя в базе было 268 проектов.

## 🐛 Найденные проблемы

1. **Неправильный путь к базе данных**
   - API использовал относительный путь `./data/database.sqlite`
   - Это указывало на `apps/api/data/database.sqlite` (пустая база)
   - Нужная база находилась в `data/database.sqlite` (корень проекта)

2. **TypeORM не мог правильно маппить данные**
   - TypeORM возвращал 0 проектов через стандартные методы
   - Пришлось использовать raw SQL запросы

3. **Проблема с синхронизацией**
   - `synchronize: true` мог пересоздавать таблицы
   - Отключено для сохранения данных

## ✅ Выполненные исправления

1. ✅ **Скопирована база данных**
   ```bash
   cp ./apps/api/database.sqlite ./data/database.sqlite
   ```

2. ✅ **Исправлен путь к базе данных**
   - Используется `process.cwd()` для получения корня проекта
   - Путь: `path.resolve(process.cwd(), 'data/database.sqlite')`
   - Или через переменную окружения: `DATABASE_PATH=../../data/database.sqlite`

3. ✅ **Использованы raw SQL запросы**
   - `getProjects()` - использует raw SQL для получения всех проектов
   - `getRegions()` - использует raw SQL для получения регионов
   - Обход проблемы маппинга TypeORM

4. ✅ **Отключена синхронизация**
   - `synchronize: false` - чтобы не пересоздавать таблицы

5. ✅ **Добавлено логирование**
   - Логирование пути к базе
   - Логирование количества проектов
   - Помогает в диагностике

## 📊 Результат

- ✅ **Проекты загружаются**: API возвращает 268 проектов
- ✅ **Регионы загружаются**: API возвращает список регионов
- ✅ **База данных подключена**: правильный путь к базе

## 🔧 Текущая конфигурация

**Путь к базе данных:**
- По умолчанию: `process.cwd()/data/database.sqlite`
- Можно переопределить через `DATABASE_PATH` в `.env`

**Методы получения данных:**
- Используются raw SQL запросы через `DataSource.query()`
- Обход проблем маппинга TypeORM

## 📝 Рекомендации

1. **Для продакшена**: Использовать абсолютный путь или переменную окружения
2. **Для разработки**: Текущая настройка работает
3. **Мониторинг**: Проверять логи на наличие ошибок подключения

## ✅ Статус

Проблема решена. Проекты из базы данных теперь загружаются корректно.

