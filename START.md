# 🚀 Запуск проекта — Sabay Owner Dashboard

## Быстрый старт

### 1. Установите зависимости
```bash
pnpm install
```

### 2. Настройте базу данных

Создайте файл `apps/api/.env.local` (уже создан, проверьте настройки):
```env
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/sabay_owner_db
JWT_SECRET=sabay-owner-dashboard-secret-key-change-in-production
JWT_EXPIRES_IN=7d
PORT=4000
NODE_ENV=development
```

### 3. Инициализируйте БД
```bash
cd apps/api
pnpm init-db
```

Это создаст:
- Администратора: `admin` / `admin123`
- Тестового инвестора: `investor1` / `investor123`
- Рыночные данные

### 4. Запустите проект

```bash
# Из корня проекта
pnpm dev
```

Или отдельно:
```bash
# API (терминал 1)
cd apps/api && pnpm dev

# Frontend (терминал 2)
cd apps/dashboard && pnpm dev
```

### 5. Откройте в браузере

- Frontend: http://localhost:3001
- API: http://localhost:4000/api

## Вход в систему

Используйте созданные учётные записи:
- **Администратор**: `admin` / `admin123`
- **Инвестор**: `investor1` / `investor123`

## Что дальше?

1. Войдите как инвестор
2. Добавьте первый объект недвижимости
3. Начните использовать систему!

Подробнее см. [DEVELOPMENT.md](./DEVELOPMENT.md)
